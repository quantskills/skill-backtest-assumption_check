---
description: 回测假设审计规则（Backtest Assumption Audit）——按九大维度独立审计回测/策略代码或研究回测报告的交易假设与偏误。
globs: ["**/*.py", "**/*.md"]
alwaysApply: false
---

# Backtest Assumption Audit

当用户要求审计回测代码 / 策略代码 / 研究回测报告时，按 QUANTSKILLS `skill-backtest-assumption_check` 执行：

1. 明确审计对象（回测代码 / 策略代码 / 研究报告 / 信号+面板）。
2. 按九大维度逐条取证：成交时点与未来函数、交易成本、涨跌停与停牌、幸存者偏差、参数自由度与多重比较、数据对齐与复权、换手与容量、基准与超额、报告透明度。
3. 每条缺陷必须给出证据（代码行号 / 脚本验证输出）。
4. 严重度分级：🔴 BLOCKER / 🟠 MAJOR / 🟡 MINOR / 🔵 INFO。
5. 输出「缺陷 × 证据 × 严重度 × 影响 × 修复」清单与总体可信度。

有 signal + 行情面板时，优先运行 `scripts/audit_cli.py` 做数据级验证（lookahead 重放 / 涨跌停可达 / 幸存者重建 / 换手容量 / 隐含成本）；缺输入时对应维度降级为「证据不足」，绝不臆测。

判定基线：T+1 开盘成交 / Top 等权 / 双边 15bp / A 股涨跌停规则（对齐生态 skill-backtest）。

合规：仅供量化研究、教育与方法论参考，不构成投资建议。
